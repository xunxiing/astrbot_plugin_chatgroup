# 主动回复系统的整体思路

## 核心是分类器

说实话，我们已经有了一个相当不错的讨论组分类器，它能做聚类分析、关键词提取、代表消息筛选，还能过滤闲聊和无价值信息，甚至可以进行时间分桶。这个工具非常强大，它的最大价值在于充当"上下文裁剪器"——确保传递给大模型的内容既与主题相关，又干净整洁。

## 上下文裁剪的艺术

裁剪内容是一门平衡的艺术：既要保留足够的信息，又不能让内容过于冗长。

我们决定保留这些内容：
- 代表性消息，能体现讨论核心
- 关键词，点明主题要点
- 最近的高价值发言，提供最新动态
- 锚点内容（链接、附件、代码块等），提供额外资源

而以下内容则可以舍弃：
- 纯粹的闲聊内容
- 短暂的情绪爆发
- 与当前主题完全无关的人的发言

我们的目标是：让模型只看到"核心讨论 + 相关参与者"的精华部分。

## 什么时候该说话？

目前计划使用llm进行判断+规则结合的方式，判断是否应该发言。

### 触发条件

**强触发情况（必须回应）：**
- 有人直接@机器人
- 出现问号+锚点的组合，表明有人在寻求具体答案
- 连续有两个人追问同一个问题

**弱触发情况（可以考虑回应）：**
- 话题中出现了新的高价值问题


## 解耦设计

最后，关于系统架构，我们坚持解耦原则：

- 上下文裁剪工作由分类器负责
- 是否发言的决定由简要判断器（结合规则和小分类器）做出
- 记忆和状态管理（如结论记录、下一步计划）则交给其他专门的插件处理，不在核心系统中耦合这些功能

## 继续讨论的具体实现

### 一、整体思路

先走硬规则：
- @ 机器人 → 不撤回
- 主题中无人回答 ≥10s → 必定说
- 已超过主题发言上限 → 不说

再走小模型（几十毫秒就能判断）：
输入：
- 机器人候选回复 embedding
- 最近 3–5 条群消息 embedding
- 辅助特征：时间间隔、是否同一个发言人、是否包含锚点

输出标签：
- 撤回（有人类同时给了几乎相同答案）
- 缩短/降级（别人回答了，但机器人答案里还有一点新信息）
- 补充（别人回答不完整，机器人输出重点差异即可）
- 继续（没人覆盖到，正常发）

### 二、判定维度

**时间维度**
- ±2s 内有人发 → 高概率撤回或降级
- ±5s 内有相关发言 → 降级或补充

**语义维度**
- 相似度 > 0.85 → 撤回
- 0.6–0.85 → 缩短/补充
- <0.6 → 保留完整发言

**信息类型维度**
- 对方发言只有"表态/确认" → 不算覆盖，可继续完整发言
- 对方发言是"步骤/结论" → 算覆盖，触发撤回/补充

### 三、动作策略

**撤回**
直接不发，或改为表情 (👍 / ✅)。

**缩短**
从原生成回复中抽取第一句结论，舍弃解释部分。
用一句确认式："对，这样做就行。"

**补充**
保留差异点，用 "另外要注意..." 开头。
长度限制在 1–2 行。

**继续**
正常输出完整回复。

## 开口时机 & 节奏

**打字态感知**：有人在打字（typing）时不开口；结束后再随机+1-4s。

**首答礼让**：检测到已有群友开始回答（连续两条相关消息内），就让出首答权，把自己降级为"补充/确认"角色。

**长度关联延迟**：对"长日志/长代码块"后的首答，多等6–10s（人类也会先读）。

**主题热度自适应**：消息频率高→延迟上限拉到15–30s；冷→5–10s。避免搅热闹。

## 轮流说话（Turn-taking）

**多说者覆盖**：近期仅被少数人主导时，倾向在新说话者插入后再开口，避免"你俩唱双簧"。

**连续发言上限**：同一主题内机器人最多连续两次发言；第三次必须等他人回应或静默≥10s。

**"不连击"原则**：不要对同一人的连续两条消息分别回复，合并为一次回应。字数限制，超过 50 字就拆开。
**结束与收束**

静默收尾：主题静默≥5–10分钟且仍未有“明确结论”时，给极短收束（1–2行：现状+下一步）。

自我降噪：收束后进入冷却（比如15分钟内不再主动），除非被@或出现新锚点问题。![alt text](image.png)